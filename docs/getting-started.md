# Getting Started

Welcome to docs-mcp.

## Installation

Run the following command in your project:

```bash
npx docs-mcp init
```

## Configuration

The `.mcp.json` file is generated automatically.
It tells Claude Code where to find the server.

## Usage

Add your Markdown files to the `docs/` folder.
Claude will use `search_docs` to query them.
